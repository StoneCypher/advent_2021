
// set your username, your name, then set configured to true

const gh_username = 'stonecypher',
      fullname    = 'John Haugeland',
      configured  = true;

export { gh_username, fullname, configured };
